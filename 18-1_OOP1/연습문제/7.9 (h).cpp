#include <iostream>
#include <array>

using namespace std;

const size_t rowSize = 2; // 2X3 배열을 선언하기 위한 전역변수
const size_t columnSize = 3;

int main() {

	array<array<int, columnSize>, rowSize> t{ 0,0,0,0,0,0 }; // 2X3 배열 선언 및 모든 elemnents들 0으로 초기화(h) for문을 사용하지 않고
}